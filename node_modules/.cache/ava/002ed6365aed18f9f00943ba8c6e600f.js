'use strict';

const test = require('ava'); //para test unitarios


test('make it pass', t => {
  t.pass();
});

/* let config = {
  logging: function(){}
}
let db = null

test.beforeEach(async() => {
  const setupDatabase = require('../lib/db.js')
  db = await setupDatabase(config)
})

test('Agent', t => {
  t.truthy(db.Agent, 'Agente existe')  //truthy que exita el valor que no sea 0
})
*/
//# sourceMappingURL=/Users/pepemoralesquiles/proyectoNode/platziverse/platziversedb/node_modules/.cache/ava/002ed6365aed18f9f00943ba8c6e600f.js.map